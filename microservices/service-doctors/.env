# /microservices\service-doctors\.env
# Service-Doctors Environment Variables
# Principle of least privilege - only doctors domain variables

# Database connection (full access to doctors-related tables)
DATABASE_URL=postgresql://predictHealth_user:password@postgres:5432/predicthealth_db

# JWT Service for token verification
JWT_SERVICE_URL=http://servicio-auth-jwt:8003

# JWT Configuration (shared secret for token verification)
JWT_SECRET_KEY=UDEM

# Service Configuration
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
LOG_LEVEL=INFO

# CORS Configuration
CORS_ORIGINS=http://localhost:5000,http://localhost:3000